# ai-infra local configuration template.
# Copy to .env and fill local values. Do not commit .env.

# Recipient for the daily news briefing Gmail draft request.
# This exact uppercase key is required by config/delivery.yaml.
AI_INFRA_BRIEFING_TO=your-address@example.com

# Local Gmail API OAuth files for draft-only local automation.
# These files should live outside the repository.
GMAIL_OAUTH_CLIENT_FILE=~/.config/ai-infra/gmail_oauth_client.json
GMAIL_OAUTH_TOKEN_FILE=~/.config/ai-infra/gmail_token.json

# auto runs the local Gmail API draft consumer after the news briefing succeeds.
# Set to false to keep only local Markdown/HTML/EML/request files.
AI_INFRA_GMAIL_API_DRAFT_ENABLED=auto
AI_INFRA_GMAIL_API_DRAFT_ATTEMPTS=3
AI_INFRA_GMAIL_API_DRAFT_RETRY_SECONDS=120

# launchd does not read ~/.zprofile. If Google APIs need a proxy on this Mac,
# put proxy variables here as well as in the interactive shell profile.
# http_proxy=http://127.0.0.1:7897
# https_proxy=http://127.0.0.1:7897
# all_proxy=socks5://127.0.0.1:7897
# no_proxy=localhost,127.0.0.1,::1

# Local Ollama server used by shared/models.py.
OLLAMA_BASE_URL=http://localhost:11434

# Keep memory pressure low on a 16GB Mac.
OLLAMA_MAX_LOADED_MODELS=1

# Model execution mode: local_only, low_cost, auto, or quality.
# Local fallback should usually stay local_only. Cloud Run production uses quality.
MODEL_MODE=local_only

# Cloud model provider for MODEL_MODE=quality. Do not commit real values.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
